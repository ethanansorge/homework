public class NQueens{
    NQueens(int size); //constructor that makes a size x size board.
    String  toString(){
	return "Test String";
    }
    String  name(){  // "last.first"  all lowercase
	return "ansorge.ethan"; 
    }
    boolean solve(){ //start from 0,0
	solve(0);
    }
    boolean solve(int x){ //start with a queen in position x of the first row or column.
        
    }
}
